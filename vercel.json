{
  "buildCommand": "cd client && rm -rf node_modules package-lock.json && npm install --include=optional && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install --prefix client",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}